/* eslint-env jasmine */
/* global flBooking */

console.log('SOMETHING IS HAPPENING');

describe('fl-booking', () => {
  it('should be defined', () => {
    expect(flBooking).toBeDefined();
  });
});
